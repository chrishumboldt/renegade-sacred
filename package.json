{
  "name": "@renegaderocks/sacred",
  "version": "0.0.3",
  "description": "An immutable value that can change over time.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist",
    "!dist/**/*.test.js",
    "!dist/**/*.test.d.ts"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "npm run clean && tsc",
    "pretest": "npm run build",
    "test": "node --test ./dist",
    "stress": "npm run build && node scripts/stress-test.js",
    "prepublishOnly": "npm test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/chrishumboldt/renegade-sacred"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "prettier": "2",
    "typescript": "^5.9.3"
  }
}
